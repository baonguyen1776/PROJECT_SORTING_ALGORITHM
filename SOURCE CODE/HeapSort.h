#ifndef HEAPSORT_H
#define HEAPSORT_H

#include <iostream>
#include <chrono>
#include <cmath>
using namespace std;
using namespace std::chrono;

double heapSortTime(int *a, int n);
unsigned long long heapSortCmp(int *a, int n);

#endif 
